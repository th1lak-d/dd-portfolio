export default function Footer() {
  return (
    <footer className="border-t border-line dark:border-line-d">
      <div className="max-w-5xl mx-auto px-6 md:px-10 py-8 flex flex-wrap items-center justify-between gap-2 font-mono text-xs text-muted dark:text-muted-d">
        <span>D/d — defense in depth</span>
        <span>built with React · deployed on GitHub Pages</span>
      </div>
    </footer>
  );
}
